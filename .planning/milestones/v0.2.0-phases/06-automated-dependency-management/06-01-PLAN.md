---
phase: 06-automated-dependency-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - renovate.json
autonomous: true
requirements:
  - DEPS-01
  - DEPS-02

must_haves:
  truths:
    - "renovate.json exists in repository root with Go module update configuration"
    - "renovate.json configures GitHub Actions dependency updates"
    - "No automerge is enabled — all PRs require manual review"
    - "Each dependency gets its own PR (no grouping)"
  artifacts:
    - path: "renovate.json"
      provides: "Renovate bot configuration for Go modules and GitHub Actions"
      contains: "gomod"
  key_links:
    - from: "renovate.json"
      to: "go.mod"
      via: "Renovate gomod manager scans go.mod for dependency updates"
      pattern: "gomod"
    - from: "renovate.json"
      to: ".github/workflows/"
      via: "Renovate github-actions manager scans workflow files for action version updates"
      pattern: "github-actions"
---

<objective>
Create a Renovate configuration file for automated dependency management.

Purpose: Go module dependencies and GitHub Actions versions should receive automated update PRs so the project stays current without manual tracking.

Output: A `renovate.json` file in the repository root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-automated-dependency-management/06-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create renovate.json with Go module and GitHub Actions configuration</name>
  <files>renovate.json</files>
  <action>
Create `renovate.json` in the repository root with the following configuration:

```json
{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "labels": ["dependencies"],
  "gomod": {
    "enabled": true
  },
  "github-actions": {
    "enabled": true
  }
}
```

Configuration choices:
- `config:recommended` preset: sensible defaults including semantic commit messages, separate PRs per dependency, no automerge
- `labels: ["dependencies"]`: tags PRs for easy filtering
- `gomod.enabled: true`: explicitly enable Go module updates (DEPS-01)
- `github-actions.enabled: true`: explicitly enable GitHub Actions updates for actions/checkout, actions/setup-go, golangci-lint-action, goreleaser-action (DEPS-02)

The `config:recommended` preset already provides:
- No automerge (D-02)
- Separate PRs per dependency (D-01)
- Reasonable scheduling (weekends for most, immediate for security)
- Semantic commit prefixes

Do NOT add automerge configuration.
Do NOT add grouping rules.
Do NOT modify any other files.
  </action>
  <verify>
    <automated>cd /Users/jorge/git/work/argo-rollouts-k6s-demo/argo-rollouts-k6-plugin && test -f renovate.json && python3 -c "import json; json.load(open('renovate.json'))" && grep -q 'gomod' renovate.json && grep -q 'github-actions' renovate.json && echo "renovate.json validation passed"</automated>
  </verify>
  <done>
- renovate.json exists in repository root
- Valid JSON with $schema reference
- Extends config:recommended preset
- gomod manager enabled (DEPS-01)
- github-actions manager enabled (DEPS-02)
- No automerge configured (D-02)
- No grouping rules — separate PRs by default (D-01)
  </done>
</task>

</tasks>

<verification>
After task completes:
1. `cat renovate.json | python3 -m json.tool` — valid JSON
2. `grep gomod renovate.json` — Go module updates configured
3. `grep github-actions renovate.json` — GitHub Actions updates configured
4. After merging to main, Renovate bot should open its onboarding PR or begin creating dependency update PRs
</verification>

<success_criteria>
- renovate.json exists with valid JSON
- Go module updates enabled (DEPS-01)
- GitHub Actions updates enabled (DEPS-02)
- No automerge (D-02)
- Separate PRs per dependency (D-01)
- Renovate bot activates after merge (human verification)
</success_criteria>

<output>
After completion, create `.planning/phases/06-automated-dependency-management/06-01-SUMMARY.md`
</output>
