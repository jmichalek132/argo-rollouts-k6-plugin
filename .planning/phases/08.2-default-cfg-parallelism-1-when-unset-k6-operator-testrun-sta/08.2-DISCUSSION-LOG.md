# Phase 08.2: Default cfg.Parallelism=1 when unset - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-18
**Phase:** 08.2-default-cfg-parallelism-1-when-unset-k6-operator-testrun-sta
**Areas discussed:** Default application site, Observability of default, Docs & examples surface, Test coverage scope

---

## Gray-Area Selection

| Option | Description | Selected |
|--------|-------------|----------|
| Default application site | Where to apply the default: buildTestRun vs ResolvedParallelism() helper vs parseConfig normalize. | ✓ |
| Observability of default | Silent default vs slog log when applying. | ✓ |
| Docs & examples surface | Godoc only, +README, or +example YAML. | ✓ |
| Test coverage scope | Which unit and e2e tests to add/keep. | ✓ |

**User's choice:** All four areas discussed.

---

## Default application site

| Option | Description | Selected |
|--------|-------------|----------|
| Inside buildTestRun | Single-line change at testrun.go:162. Keeps pure function pure; no validation/parse changes. | ✓ |
| New PluginConfig.ResolvedParallelism() helper | Method on PluginConfig; premature abstraction without a second consumer. | |
| Normalize in parseConfig | Mutate cfg.Parallelism=1 at parse layer. Breaks the existing "0 means unset" test contract. | |

**User's choice:** Inside buildTestRun.
**Notes:** Scoped to construction site where the k6-operator semantics live. No change to validation or parse. Captured as **D-01**.

---

## Observability of default

| Option | Description | Selected |
|--------|-------------|----------|
| Silent default | No log. Keep buildTestRun pure; verify via kubectl get testrun or unit tests. | ✓ |
| slog.Info on default | Emit structured log; adds logger dep and side effect to a pure builder. | |
| slog.Debug on default | Debug-level variant with same side-effect concern. | |

**User's choice:** Silent default.
**Notes:** Pure builder convention from Phase 8 D-06 preserved. Captured as **D-02**.

---

## Docs & examples surface

| Option | Description | Selected |
|--------|-------------|----------|
| Godoc only | Update PluginConfig.Parallelism and buildTestRun godoc. No README/example changes. | |
| Godoc + README note | Godoc update plus one-line note in examples/k6-operator/README.md. | ✓ |
| Godoc + explicit YAML in examples | Set parallelism: 1 in example AnalysisTemplate YAML. | |

**User's choice:** Godoc + README note.
**Notes:** README discoverability without hiding the default by setting it explicitly in YAML. Captured as **D-07** (Godoc) and **D-08** (README note).

---

## Test coverage scope

| Option | Description | Selected |
|--------|-------------|----------|
| New TestBuildTestRun_DefaultsParallelismWhenUnset | Primary regression for the new default behavior. | ✓ |
| New TestBuildTestRun_PreservesExplicitParallelism | Pins the "only zero defaults" rule; partial overlap with existing test. | ✓ |
| Keep TestValidateK6Operator_ZeroParallelism semantics | Validation layer remains authoritative on "0 is valid". | ✓ |
| Add e2e regression for parallelism=0 | Full kind-cluster flow with parallelism unset; unblocks Phase 10. | ✓ |

**User's choice:** All four.
**Notes:** Unit tests (D-04) + validation boundary preservation (D-05) + e2e regression (D-06) together form the complete coverage.

---

## Claude's Discretion

- Exact wording of new test function comments
- Whether the default is applied via local var, a small helper in the same file, or inline — as long as the rule `cfg.Parallelism == 0 → 1` is clear at the call site
- README.md subsection placement / heading style
- Whether the e2e regression is a new function or a parameterization of an existing test

## Deferred Ideas

- slog warning when default applied (may revisit if operators report confusion)
- PluginConfig.ResolvedParallelism() helper (revisit if a second caller appears)
- Explicit parallelism: 1 in example YAML (defeats the point of the default)
- Centralized PluginConfig.ApplyDefaults() (no other field currently needs it)
- Upstream k6-operator fix to default Parallelism=1 (out of project scope)
