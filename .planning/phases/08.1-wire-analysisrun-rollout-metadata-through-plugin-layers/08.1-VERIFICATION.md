---
phase: 08.1-wire-analysisrun-rollout-metadata-through-plugin-layers
verified: 2026-04-17T12:00:00Z
status: human_needed
score: 9/9 decisions verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Deploy fix to a real kind/k3s cluster and run the Phase 10 e2e test suite against the k6-operator backend"
    expected: "TestRun/PrivateLoadZone CRs are created in the same namespace as the AnalysisRun (not default), ConfigMap lookup succeeds on the first Run, OwnerReferences stamp carries kind=AnalysisRun UID (metric path) or kind=Rollout UID (step path), and GC cascades when parent is deleted"
    why_human: "The e2e tests are the original reproducer of this bug; verifying the fix requires a live cluster with Argo Rollouts + k6-operator deployed, which the unit test suite cannot emulate. The fake dynamic client used in unit tests does not simulate kube-apiserver OwnerReference validation or real ConfigMap resolution ordering."
  - test: "Run the step plugin in a rollout that sets no explicit cfg.Namespace and verify cfg.Namespace flows from rollout.Namespace into the created TestRun"
    expected: "Created TestRun.metadata.namespace matches the Rollout's namespace; no 'default' fallback is hit at the operator layer in normal operation"
    why_human: "D-02 preserves the 'default' fallback as a dead-code safety net; exercising real end-to-end flow is the only way to confirm the new wiring makes that fallback unreachable in practice."
  - test: "Run a canary with both a step plugin AND an AnalysisTemplate metric plugin triggered from the same Rollout; delete the AnalysisRun during the run"
    expected: "The TestRun CR created by the metric plugin is GC'd (AR owner); the TestRun CR created by the step plugin survives (Rollout owner). Confirms D-07 precedence behaves correctly under real cascading GC."
    why_human: "Kubernetes garbage collection is driven by the real controller watching OwnerReferences; unit tests assert the OwnerReference is stamped correctly but cannot observe the real GC cascade."
---

# Phase 08.1: Wire AnalysisRun/Rollout metadata through plugin layers - Verification Report

**Phase Goal:** Wire AnalysisRun/Rollout metadata through plugin layers - populate `cfg.Namespace`, `cfg.RolloutName`, `cfg.AnalysisRunName`, `cfg.AnalysisRunUID`, and new `cfg.RolloutUID` in metric.go and step.go before dispatch. Fix operator-provider owner-reference construction (AR > Rollout > none via `parentOwnerRef(cfg)`).

**Verified:** 2026-04-17T12:00:00Z
**Status:** human_needed
**Re-verification:** No - initial verification

## Goal Achievement

### Observable Truths (9 Locked Decisions)

| #   | Decision | Truth | Status | Evidence |
| --- | -------- | ----- | ------ | -------- |
| 1 | **D-01** | Namespace precedence: user cfg.Namespace > parent > "default" | VERIFIED | `metric.go:235` and `step.go:271` both gate the overwrite with `if cfg.Namespace == ""`; verified by `TestRun_UserNamespaceWinsOverAR` and `TestRun_UserNamespaceWinsOverRollout` |
| 2 | **D-02** | Existing `"default"` fallback in operator.go readScript() stays as safety net | VERIFIED | `operator.go:146` and `operator.go:219` both retain `if ns == "" { ns = "default" }`; two occurrences unchanged |
| 3 | **D-03** | Metric plugin walks ar.OwnerReferences for Kind==Rollout AND Controller==true | VERIFIED | `metric.go:240` has exact predicate `owner.Kind == "Rollout" && owner.Controller != nil && *owner.Controller`; test `TestRun_PrefersControllerRolloutOwnerRef` (metric_test.go:233) proves non-controller ref cannot hijack |
| 4 | **D-04** | Standalone AR (no controller Rollout ref) warns and leaves RolloutName empty | VERIFIED | `metric.go:246` emits `slog.Warn("standalone AnalysisRun (no Rollout owner ref); RolloutName left empty", ...)`; test `TestRun_StandaloneAnalysisRun` (metric_test.go:189) asserts empty RolloutName |
| 5 | **D-05** | PluginConfig has RolloutUID string with `json:"-"`, only step.go sets it | VERIFIED | `config.go:45` has `RolloutUID string \`json:"-"\``; `step.go:270` sets `cfg.RolloutUID = string(rollout.UID)`; metric.go grep for `RolloutUID` returns 0 matches (confirmed) |
| 6 | **D-06** | step.go sets cfg.RolloutName = rollout.Name; no owner-ref walk | VERIFIED | `step.go:269` `cfg.RolloutName = rollout.Name`; no OwnerReferences walk in step.go (grep confirmed) |
| 7 | **D-07** | Owner-ref precedence: AR > Rollout > none in parentOwnerRef(cfg) | VERIFIED | `testrun.go:79-97` encodes early-return chain: `AnalysisRunUID != ""` first, then `RolloutUID != ""`, else nil; tests `TestTriggerRun_WithRolloutUID` and `TestTriggerRun_AnalysisRunUIDWinsOverRolloutUID` (operator_test.go:347, 381) verify both branches and precedence |
| 8 | **D-08** | Existing nil-AR/nil-Rollout tests preserved unchanged | VERIFIED | Only `GarbageCollect(_ *v1alpha1.AnalysisRun, ...)` remains with `_` binding (metric.go:59); all 3 Run/Resume/Terminate bind `ar`; full metric + step test suites (31 + 28 tests) pass |
| 9 | **D-09** | Nil AR/Rollout emits slog.Warn and skips extraction | VERIFIED | `metric.go:226` `if ar == nil { slog.Warn("nil AnalysisRun received; ...") }`; `step.go:262` `if rollout == nil { slog.Warn("nil Rollout received; ...") }` |

**Score:** 9/9 decisions verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `internal/provider/config.go` | RolloutUID string field, json:"-" | VERIFIED | Line 45 has the field with exact tag and comment; sits in the injected-fields block aligned with its three siblings |
| `internal/provider/operator/testrun.go` | parentOwnerRef(cfg) helper encoding AR > Rollout > none | VERIFIED | Lines 79-97 define the helper with R2 invariant doc comment; both `buildTestRun` (L153) and `buildPrivateLoadZone` (L195) call it; old `analysisRunOwnerRef` fully removed (0 matches repo-wide) |
| `internal/metric/metric.go` | populateFromAnalysisRun helper + 3 call sites in Run/Resume/Terminate | VERIFIED | Helper at L225-250; call sites at L79, L117, L187 (3 total, matches expectation); signatures bind `ar` not `_` (except GarbageCollect which stays `_` per D-14) |
| `internal/step/step.go` | populateFromRollout helper + Run + stopActiveRun call sites; stopActiveRun signature includes rollout | VERIFIED | Helper at L261-274; call sites at L80 and L224 (2 total, matches plan); stopActiveRun signature at L215 takes `rollout *v1alpha1.Rollout` as first param; Terminate (L201) and Abort (L207) both pass rollout through |
| `internal/metric/metric_test.go` | testAR fixture + 5 new tests (R3, R4, 3 baseline) | VERIFIED | testAR at L53; TestRun_PopulatesCfgFromAnalysisRun (L169), TestRun_StandaloneAnalysisRun (L189), TestRun_UserNamespaceWinsOverAR (L209), TestRun_PrefersControllerRolloutOwnerRef (L233, R3), TestResume_ExtractsAnalysisRunMetadata (L285, R4) |
| `internal/step/step_test.go` | testRollout fixture + 3 new tests (R6 + 2 baseline) | VERIFIED | testRollout helper present; TestRun_PopulatesCfgFromRollout (L183), TestRun_UserNamespaceWinsOverRollout (L212), TestTerminate_PassesPopulatedConfigToStopRun (L247, R6) |
| `internal/provider/operator/operator_test.go` | 2 new tests (Rollout owner-ref + AR-wins precedence) | VERIFIED | TestTriggerRun_WithRolloutUID (L347), TestTriggerRun_AnalysisRunUIDWinsOverRolloutUID (L381); existing TestTriggerRun_WithAnalysisRunUID and TestTriggerRun_WithoutAnalysisRunUID unchanged |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `testrun.go buildTestRun` | `parentOwnerRef(cfg)` | `OwnerReferences:` field | WIRED | Line 153: `OwnerReferences: parentOwnerRef(cfg)` |
| `testrun.go buildPrivateLoadZone` | `parentOwnerRef(cfg)` | `OwnerReferences:` field | WIRED | Line 195: `OwnerReferences: parentOwnerRef(cfg)` |
| `metric.go Run` | `provider.TriggerRun` | cfg with Namespace/RolloutName/AnalysisRunName/AnalysisRunUID populated | WIRED | L79 `populateFromAnalysisRun` called before L90 `TriggerRun(ctx, cfg)` |
| `metric.go Resume` | `provider.GetRunResult` | cfg populated | WIRED | L117 `populateFromAnalysisRun` before L130 `GetRunResult(ctx, cfg, runID)` |
| `metric.go Terminate` | `provider.StopRun` | cfg populated | WIRED | L187 `populateFromAnalysisRun` before L197 `StopRun(ctx, cfg, runID)` |
| `step.go Run` | `provider.TriggerRun` | cfg with Namespace/RolloutName/RolloutUID populated | WIRED | L80 `populateFromRollout` before L112 `TriggerRun(triggerCtx, cfg)` |
| `step.go Terminate` | `stopActiveRun(rollout, ctx, "terminate")` | rollout argument propagation | WIRED | L202 passes rollout through |
| `step.go Abort` | `stopActiveRun(rollout, ctx, "abort")` | rollout argument propagation | WIRED | L208 passes rollout through |
| `step.go stopActiveRun` | `provider.StopRun` | cfg populated via populateFromRollout | WIRED | L224 populateFromRollout before L242 StopRun call |
| `metric.go owner-ref walk` | D-03 predicate | `Kind == "Rollout" && Controller != nil && *Controller` | WIRED | L240 has the exact predicate enforcing controller preference |

### Data-Flow Trace (Level 4)

Not applicable — this phase modifies plugin library code (no dynamic rendering or data fetch patterns). The "data flow" being verified is config-population from parent objects into provider calls, which is already covered by the Key Link Verification above with closure-capture assertions in the mock provider tests.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Module builds cleanly | `go build ./...` | exit 0 | PASS |
| Module passes vet | `go vet ./...` | exit 0 | PASS |
| Full test suite passes | `go test -count=1 ./...` | 6 packages ok; all tests pass | PASS |
| metric package tests | `go test -count=1 ./internal/metric/...` | ok, 0.281s | PASS |
| step package tests | `go test -count=1 ./internal/step/...` | ok, 0.233s | PASS |
| operator package tests | `go test -count=1 ./internal/provider/operator/...` | ok, 0.668s | PASS |
| provider package tests | `go test -count=1 ./internal/provider/...` | ok, 0.426s | PASS |
| Old analysisRunOwnerRef helper fully removed | grep repo-wide | 0 matches | PASS |
| Only GarbageCollect still binds `_ *v1alpha1.AnalysisRun` or `_ *v1alpha1.Rollout` | grep repo-wide | 1 match (metric.go:59 GarbageCollect) | PASS |
| RolloutUID set only in step.go (not metric.go) | grep in metric.go | 0 RolloutUID writes | PASS |

### Requirements Coverage

Per verification request: phase has `requirements: []` — no REQ-IDs mapped (bug fix inserted post-Phase-10). REQUIREMENTS.md not cross-referenced. N/A.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| internal/metric/metric.go | 246-249 | Ambiguous warning message when AR has non-controller Rollout owner ref or non-Rollout owner refs | Info | Flagged in 08.1-REVIEW.md as IN-01; operator-facing log readability suggestion only; does not affect behavior |
| internal/step/step.go | 261-274 | `populateFromRollout` does not defend against empty `rollout.Name` when `rollout.UID` is set | Info | Flagged in 08.1-REVIEW.md as IN-02; relies on kube-apiserver to reject empty-Name OwnerReference; hardening suggestion only |
| internal/provider/operator/operator_test.go | 347-379 | `TestTriggerRun_WithRolloutUID` does not lock Controller/BlockOwnerDeletion nil state | Info | Flagged in 08.1-REVIEW.md as IN-03; test hardening suggestion only |

No blocker or warning-severity anti-patterns. The three INFO items are all carried forward from `08.1-REVIEW.md` and are intentional trade-offs or optional hardening.

### Revision Gate Evidence (from 08.1-REVIEWS.md)

All 8 revisions from the codex pass-1 review have landed:

| Revision | Location | Status |
| -------- | -------- | ------ |
| **R1** (Plan 01: file-location note) | 08.1-01-PLAN.md `<file_location_note>` | LANDED — plan clearly targets `testrun.go` not `operator.go` |
| **R2** (Plan 01: parentOwnerRef invariant doc) | testrun.go:75-78 | LANDED — "Caller invariant: caller MUST set both the UID and the matching Name" |
| **R3** (Plan 02: Controller==true preference test) | metric_test.go:233 | LANDED — `TestRun_PrefersControllerRolloutOwnerRef` builds a 2-entry fixture with non-controller first |
| **R4** (Plan 02: Resume-path test) | metric_test.go:285 | LANDED — `TestResume_ExtractsAnalysisRunMetadata` asserts on `GetRunResultFn` closure |
| **R5** (Plan 02: optional slog expansion) | metric.go:98-105, 168-176 | LANDED — `namespace`, `rollout`, `analysisRun` fields added to Run and Resume slog.Info |
| **R6** (Plan 03: Terminate StopRun propagation test) | step_test.go:247 | LANDED — `TestTerminate_PassesPopulatedConfigToStopRun` asserts StopRun receives populated cfg |
| **R7** (Plan 03: behavior over grep) | 08.1-03-PLAN.md `<verification>` | LANDED — plan replaces brittle grep count with R6 behavior check |
| **R8** (Plan 03: nil-Rollout graceful degradation) | step.go:256-258 | LANDED — doc comment calls out "nil rollout preserves pre-fix behavior ... provider-specific" |

### Human Verification Required

Three items require a real cluster to verify end-to-end — detailed in frontmatter `human_verification:` array:

1. **Deploy to cluster and run Phase 10 e2e tests** — Confirms the namespace-fallback bug that motivated this phase is actually fixed in a real controller/kube-apiserver flow (unit tests use fake clients that don't simulate real ConfigMap resolution or OwnerReference server-side validation).
2. **Step plugin with no explicit namespace** — Confirms rollout.Namespace flows end-to-end and the `"default"` fallback at operator.go:146/219 is now dead code in practice.
3. **Combined AR + step GC cascade** — Confirms D-07 precedence behavior under real Kubernetes garbage collection (AR-owned CRs should GC when AR deleted; Rollout-owned CRs should survive AR deletion).

### Gaps Summary

No automated gaps found. All 9 locked decisions (D-01..D-09) are implemented and exercised by tests. The full test suite (`go test -count=1 ./...`) passes on all 6 packages. Build and vet are clean. All 8 pass-1 codex revisions have landed.

The three INFO-level items from `08.1-REVIEW.md` are intentional trade-offs or optional hardening and do not block the phase. They may be addressed opportunistically in future phases (suggested minor follow-ups, not regressions).

Status is `human_needed` — not `passed` — because the original bug the phase fixes (namespace fallback to `"default"`) was discovered by Phase 10 e2e tests against a real cluster. Full confidence that the fix works end-to-end requires re-running those e2e tests on a live cluster, which is outside the scope of unit-test-level verification.

---

_Verified: 2026-04-17T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
