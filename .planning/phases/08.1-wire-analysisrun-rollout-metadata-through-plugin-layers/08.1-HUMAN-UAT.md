---
status: complete
phase: 08.1-wire-analysisrun-rollout-metadata-through-plugin-layers
source: [08.1-VERIFICATION.md]
started: 2026-04-17T12:00:00Z
updated: 2026-04-20T10:20:00Z
resolution: Tests 1 and 2 passed via 2026-04-20 e2e run; test 3 deferred as tech debt (see Gaps). No blocker for v0.3.0 milestone close.
---

## Current Test

[no active test — tests 1 and 2 resolved by 2026-04-20 e2e run; test 3 deferred]

## Tests

### 1. Phase 10 e2e suite re-run on kind/k3s cluster

expected: TestRun/PrivateLoadZone CRs are created in the same namespace as the AnalysisRun (not `default`), ConfigMap lookup succeeds on the first `Run`, OwnerReferences stamp carries `kind=AnalysisRun` UID (metric path) or `kind=Rollout` UID (step path), and GC cascades when parent is deleted.
result: passed (2026-04-20)
evidence: Full `make test-e2e` run on 2026-04-20 — TestK6OperatorStepPass 62.12s PASS, TestK6OperatorMetricPass 9.52s PASS. Both tests execute in the kind-cluster test namespace `k6-e2e-*`; TestRun CRs are created with OwnerReferences and reconciled by k6-operator end-to-end. The first-Run ConfigMap lookup succeeds (previously failed with "default" namespace fallback pre-08.1). Test suite total: 6/6 PASS, 0 FAIL, 261s.

### 2. Step plugin with no explicit cfg.Namespace

expected: Created TestRun `metadata.namespace` matches the Rollout's namespace; no `default` fallback is hit at `operator.go:146/219` in normal operation.
result: passed (2026-04-20)
evidence: `TestK6OperatorStepPass` fixture `e2e/testdata/rollout-step-k6op.yaml` sets no `namespace` in the plugin step config. Test passed, proving `cfg.Namespace` flows from the Rollout's namespace through the step plugin layer into `buildTestRun` — the `default` fallback at `operator.go:146/219` is never reached in normal operation.

### 3. Combined AR + step TestRun GC cascade

expected: Running a canary with both a step plugin AND an AnalysisTemplate metric plugin from the same Rollout, then deleting the AnalysisRun during the run, produces: metric-created TestRun CR is GC'd (AR owner); step-created TestRun CR survives (Rollout owner). Confirms D-07 precedence behaves correctly under real Kubernetes GC.
result: deferred (tech debt — no automated coverage; low-risk edge case not on v0.3.0 critical path)
evidence: N/A — not exercised by the current e2e suite. OwnerReference precedence (D-07: AR > Rollout) is covered by unit tests in `internal/provider/operator/` (parentOwnerRef helper), but the Kubernetes GC cascade in a combined-run scenario is not automatically tested. Deferred to a future phase that extends the e2e suite with a combined-run + AR-deletion scenario.

## Summary

total: 3
passed: 2
issues: 0
pending: 0
deferred: 1
skipped: 0
blocked: 0

## Gaps

- Test 3 deferred — combined step+metric canary with mid-run AR deletion is not exercised by automated e2e. Tracked as tech debt; not a v0.3.0 blocker because OwnerReference construction is unit-tested (D-07 precedence in `parentOwnerRef`) and the Kubernetes GC mechanism is provided by kube-apiserver (not plugin code). A follow-up phase may extend `e2e/k6_operator_test.go` with this scenario.
